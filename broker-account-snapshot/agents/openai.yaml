interface:
  display_name: "User-Supplied Account Snapshot"
  short_description: "Parse XTB values supplied by the user"
  default_prompt: "Use $broker-account-snapshot only to parse XTB/account text, screenshots, or exports supplied by the user into a reusable session snapshot; never connect to or control XTB."
policy:
  allow_implicit_invocation: false
