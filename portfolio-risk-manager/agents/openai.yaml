interface:
  display_name: "Manual Trade Risk Planner"
  short_description: "Build risk geometry before optional sizing"
  default_prompt: "Use $portfolio-risk-manager to calculate public-reference entry, stop, targets, and reward-to-risk now; add XTB quantity only from current contract and account values directly supplied by the user."
